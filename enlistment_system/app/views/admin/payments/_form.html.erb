<%= simple_form_for([@payment.student, @payment]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :amount_paid %>
    <%= f.association :assessment, as: :hidden %>
  </div>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>

  





<% end %>
